# When Did I

## Setup

1. Create a `.env` file at the root of the project directory and set these enviornment variables:

    * `MONGO_DB_URI` - the url for the mongodb database which has the database name and password included in it.
    * `EXPRESS_PORT` - the port that express will run on
